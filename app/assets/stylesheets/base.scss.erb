@import 'fonts';
@import 'normalize';

html {
  -webkit-font-smoothing: antialiased;
  background: #fffff8;
}

.container {
  max-width: 750px;
  margin: 0 auto;
  padding: 0 16px;
}

<%= Rouge::Themes::Github.render(scope: '.e-content .highlight') %>
